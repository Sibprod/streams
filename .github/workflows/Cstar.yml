name: Cstar action

on:
  schedule:
    - cron: '46 */6 * * *'
  
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3

      - name: config
        run: |
          git config --global user.email "<>"
          git config --global user.name "Cstar bot"
      
      - name: updating cstar
        run: python3 ressources/dm/py/Cstar.py > ressources/dm/py/hls/cstar.m3u8
        
      - name: git add
        run: |
          git add -A
          ls -la 
          
      - name: commit & push
        run: |
          git pull origin master
          git commit -m "cstar updated"
          git push origin master
